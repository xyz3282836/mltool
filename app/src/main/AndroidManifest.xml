<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.minigee.app">

    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS"/>

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".base.BaseApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.Main"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.DrawerLayoutTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.Icon"></activity>
        <activity android:name=".ui.Login"></activity>
        <activity android:name=".ui.Reg"></activity>
        <activity android:name=".ui.Setting"></activity>
        <activity android:name=".ui.Demo"></activity>


        <receiver android:name=".broadcast.SMSBroadcastReceiver">        //继承BroadcastReceiver 的类名

            <!-- 設定要捕捉的訊息名稱為provider中Telephony.SMS_RECEIVED -->
            <intent-filter>
                <action
                    android:name="android.provider.Telephony.SMS_RECEIVED" /> 	//在BroadcastReceiver 创建的action
            </intent-filter>
        </receiver>
    </application>

</manifest>
